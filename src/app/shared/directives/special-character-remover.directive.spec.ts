/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SpecialCharacterRemoverDirective } from './special-character-remover.directive';

describe('Directive: SpecialCharacterRemover', () => {
  it('should create an instance', () => {
    const directive = new SpecialCharacterRemoverDirective();
    expect(directive).toBeTruthy();
  });
});
