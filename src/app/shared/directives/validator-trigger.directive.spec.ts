/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ValidatorTriggerDirective } from './validator-trigger.directive';

describe('Directive: ValidatorTrigger', () => {
  it('should create an instance', () => {
    const directive = new ValidatorTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
