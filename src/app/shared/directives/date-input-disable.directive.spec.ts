/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateInputDisableDirective } from './date-input-disable.directive';

describe('Directive: DateInputDisable', () => {
  it('should create an instance', () => {
    const directive = new DateInputDisableDirective();
    expect(directive).toBeTruthy();
  });
});
